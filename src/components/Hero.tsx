import HeroBtns from "./HeroBtns";

import { HeroContent } from "./HeroContent";

const Hero = () => {
  return (
    <section className="hero-container">
      <HeroContent />

      <HeroBtns />
    </section>
  );
};

export default Hero;
